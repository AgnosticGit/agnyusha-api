NODE_ENV=development
PORT=3001

DATABASE_URL=postgresql://agnyusha:agnyusha@localhost:5433/agnyusha?schema=public

CORS_ORIGIN=http://localhost:3000
# Public URL is applied automatically by `npm run dev:ngrok`
# (domain in agnyusha-web/tunnel.config.json).

# CDEK — тестовый контур по умолчанию
# Документация: https://apidoc.cdek.ru/
# Account / Secure password из кабинета интегратора СДЭК (не логин ЛК)
CDEK_API_URL=https://api.edu.cdek.ru
CDEK_CLIENT_ID=your_cdek_account
CDEK_CLIENT_SECRET=your_cdek_secure_password

# Yandex Delivery NDD (токен из кабинета: Интеграция)
# Docs: https://yandex.ru/support/delivery-profile/ru/api/other-day/access
# Тест: https://b2b.taxi.tst.yandex.net
# Прод: https://b2b-authproxy.taxi.yandex.net
YANDEX_DELIVERY_API_URL=https://b2b.taxi.tst.yandex.net
YANDEX_DELIVERY_TOKEN=your_yandex_bearer_token
# Склад отгрузки (source). Для tst: fbed3aa1-2cc6-4370-ab4d-59c5cc9bb924
YANDEX_PLATFORM_STATION_ID=your_yandex_platform_station_id

# Auth / email (Resend)
# from: только ASCII в имени; тест — onboarding@resend.dev (после верификации домена — свой)
MAIL_DRIVER=resend
MAIL_FROM=Agnyusha <onboarding@resend.dev>
RESEND_API_KEY=
MAGIC_LINK_EXPIRES_MINUTES=15
# Stock / inventory (cart caps, OOS checks, order decrement, admin inventory).
# Default off. Set true to enable.
INVENTORY_ENABLED=false

# Google OAuth (callback must hit the API host so the session cookie is set)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback

# Ozon Pay (test token works the same as prod — only cabinet mode differs)
# Docs host: https://payapi.ozon.ru/v1
# Cabinet URLs (use public ngrok host of the web app):
# Success: https://<ngrok-domain>/payment/success
# Fail:    https://<ngrok-domain>/payment/fail
# Notify:  https://<ngrok-domain>/api/payments/ozon/webhook
# Note: free ngrok often blocks server-to-server webhooks; on success redirect
# the app also confirms payment via getOrderDetails.
OZON_PAY_API_URL=https://payapi.ozon.ru/v1
OZON_PAY_ACCESS_KEY=
OZON_PAY_SECRET_KEY=
OZON_PAY_NOTIFICATION_SECRET=
# Public storefront URL for success/fail/notify (defaults to first CORS_ORIGIN)
PUBLIC_WEB_URL=http://localhost:3000
