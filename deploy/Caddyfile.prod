# Same as Caddyfile once the domain is live.
# Kept for reference / rollback docs in README.

{$DOMAIN}, www.{$DOMAIN} {
	encode gzip zstd

	handle /api* {
		reverse_proxy api:3001
	}

	handle /uploads* {
		reverse_proxy api:3001
	}

	handle {
		reverse_proxy web:3000
	}
}
