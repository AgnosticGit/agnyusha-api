# Temporary HTTP (IP or domain without TLS).
# When DNS is ready, replace with Caddyfile.prod contents and set DOMAIN in .env.

:80 {
	encode gzip zstd

	handle /api* {
		reverse_proxy api:3001
	}

	handle /uploads* {
		reverse_proxy api:3001
	}

	handle {
		reverse_proxy web:3000
	}
}
